**SLIP 5_1**
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
df = pd.read_csv(r'C:\Users\Dell\Downloads\archive (14)\Fuel_Consumption_2000-2022.csv')
df
x = df[['ENGINE SIZE','CYLINDERS','FUEL CONSUMPTION']]
y = df['EMISSIONS']
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.2, random_state=0)
model = LinearRegression().fit(x_train, y_train)
print("Predicted CO2 Emission:", round(model.predict([[3.5,6,10.5]])[0], 2))


**SLIP 5_2**
from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split
from sklearn.neighbors import KNeighborsClassifier
x, y = load_iris(return_X_y=True)
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.2, random_state=0)
model = KNeighborsClassifier(n_neighbors=3).fit(x_train, y_train)
print("Predicted Class:", model.predict([x_test[0]])[0])
